# ============================================================
# 🚀 AndroidWhispers — Smart CI/CD Release + Wiki + Pages + QR v1.2.2 (Final Debug Enhanced)
# ------------------------------------------------------------
# ✅ Fully dedicated for AndroidWhispers project
# ✅ Smart Detect (diff-aware) + Force Build mode
# ✅ Builds, publishes APK, updates Wiki & GH Pages automatically
# ✅ Detailed debug logs for task detection, model handling, and Wiki
# ============================================================

name: AndroidWhispers — Smart Release v1.2.2 (Final Debug)

on:
  workflow_dispatch:
    inputs:
      enable_review:
        description: "Run Lint & Unit Tests before build"
        type: boolean
        default: true
      force_build:
        description: "Force rebuild regardless of diff detection"
        type: boolean
        default: false

permissions:
  contents: write
  pages: write

defaults:
  run:
    shell: bash

env:
  OWNER: "ishizuki-tech"
  REPO_NAME: "AndroidWhispers"
  REPO: "ishizuki-tech/AndroidWhispers"
  BASE_URL: "https://ishizuki-tech.github.io/AndroidWhispers"
  WIKI_PAGE_URL: "https://github.com/ishizuki-tech/AndroidWhispers/wiki/Home"
  NDK_VERSION: 28.1.13356709
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
  ANDROID_HOME: /usr/local/lib/android/sdk
  _JAVA_OPTIONS: "-Xmx4g"
  GRADLE_OPTS: "-Xmx4g -Dorg.gradle.daemon=true"

# ============================================================
# 🧠 Smart Detect
# ============================================================
jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.detect.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - id: detect
        run: |
          set -Eeuo pipefail
          if [ "${{ inputs.force_build }}" = "true" ]; then
            echo "⚙️ Force Build enabled — skipping diff detection."
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "🔍 Checking for source changes..."
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            echo "🆕 First-time build detected."
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          CHANGED=$(git diff --name-only "$LAST_TAG" HEAD | grep -E '^(app/|nativelib/|.*\.(kt|kts|cpp|hpp|h))' || true)
          if [ -n "$CHANGED" ]; then
            echo "✅ Source has changed since $LAST_TAG"
            echo "$CHANGED"
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "🟡 No source changes since $LAST_TAG"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

  # ============================================================
  # 🏗️ Build
  # ============================================================
  build:
    runs-on: ubuntu-latest
    needs: detect
    if: ${{ needs.detect.outputs.changed == 'true' }}
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
          ref: ${{ github.ref_name }}

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: android-actions/setup-android@v3
        with:
          ndk-version: ${{ env.NDK_VERSION }}

      - name: 🧩 Add Swap Memory (4GB)
        run: |
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          echo "✅ Swap memory added successfully."
          free -h

      - name: 📦 Download Whisper Models (if task exists, with debug)
        run: |
          set -e
          echo "🔍 Checking for Gradle task ':app:downloadModel' ..."
          echo "🧩 Gradle version:"
          ./gradlew -v || true
          echo "🗂️ Listing available tasks in :app module..."
          ./gradlew -p app tasks --all | tee gradle_tasks.log || true

          if grep -q "downloadModel" gradle_tasks.log; then
            echo "✅ Found task ':app:downloadModel'. Running..."
            echo "-------------------------------------------"
            ./gradlew :app:downloadModel --stacktrace --no-daemon
            echo "-------------------------------------------"
            echo "🎉 Model download completed successfully."
          else
            echo "⚠️ Task ':app:downloadModel' not found. Skipping model download."
            echo "📁 Debug info:"
            echo "Current directory: $(pwd)"
            echo "Submodules: $(ls -1)"
            echo "-------------------------------------------"
            echo "If your app/build.gradle.kts defines downloadModel,"
            echo "ensure it's within the ':app' module and Gradle sync succeeds locally."
          fi

      - name: 🧪 Lint & Unit Tests
        if: ${{ inputs.enable_review == true }}
        run: |
          ./gradlew :app:lintDebug || echo "::warning::Lint warnings"
          ./gradlew :app:testDebugUnitTest || echo "::warning::Unit tests failed"

      - name: 🏗️ Build Release APK
        run: |
          ./gradlew clean :app:assembleRelease --stacktrace --no-daemon
          mkdir -p release
          APK=$(find app/build/outputs -type f -name "*release*.apk" | head -n1)
          if [ -z "$APK" ]; then
            echo "❌ APK not found!"
            exit 1
          fi
          cp "$APK" release/app-release.apk
          echo "✅ APK ready at release/app-release.apk"

      # ------------------------------------------------------------
      # 🧠 Generate meta/models.json with model info
      # ------------------------------------------------------------
      - name: 🧠 Generate meta/models.json (with model info)
        id: meta
        run: |
          set -e
          mkdir -p meta
          DATE=$(date '+%Y-%m-%d %H:%M')
          VERSION=$(grep -E 'versionName' app/build.gradle.kts | sed -E 's/.*"([^"]+)".*/\1/' | head -n1 || echo "1.0")
          TAG="v${VERSION}-$(date '+%Y%m%d-%H%M')"

          MODEL_DIR="app/src/main/assets/models"
          if [ -d "$MODEL_DIR" ]; then
            echo "📦 Collecting model files..."
            MODELS_JSON=$(find "$MODEL_DIR" -type f -name "*.bin" -exec bash -c '
              for f; do
                name=$(basename "$f")
                size=$(du -h "$f" | cut -f1)
                echo "{\"name\":\"$name\",\"size\":\"$size\"}"
              done
            ' bash {} + | jq -s '.')
          else
            MODELS_JSON="[]"
          fi

          jq -n \
            --arg tag "$TAG" \
            --arg date "$DATE" \
            --argjson models "$MODELS_JSON" \
            '{version_tag:$tag, generated:$date, models:$models}' > meta/models.json

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "✅ Generated meta/models.json:"
          cat meta/models.json

      - uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: |
            release/app-release.apk
            meta/models.json
          retention-days: 7

  # ============================================================
  # 🚀 Publish (Release + Wiki + GH Pages)
  # ============================================================
  publish:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🧹 Clean Workspace
        run: |
          echo "🧹 Removing old wiki/pages/meta..."
          rm -rf wiki gh-pages artifacts/meta || true

      - uses: actions/download-artifact@v4
        with:
          name: release-apk
          path: artifacts

      - name: 🚀 Publish GitHub Release (latest)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "latest"
          name: "AndroidWhispers — Latest Build"
          draft: false
          prerelease: false
          files: artifacts/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📘 Ensure Wiki Exists (master)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          WIKI_URL="https://github.com/${REPO}.wiki.git"
          echo "🔎 Checking Wiki (master branch)..."
          if git ls-remote "$WIKI_URL" &>/dev/null; then
            echo "✅ Wiki already exists."
          else
            echo "🆕 Creating new Wiki repository (master)..."
            mkdir -p wiki && cd wiki
            git init
            echo "# 📘 AndroidWhispers Wiki" > README.md
            echo "Auto-generated by CI/CD." >> README.md
            git config user.name "github-actions"
            git config user.email "actions@github.com"
            git add .
            git commit -m "🆕 Initial Wiki setup"
            git branch -M master
            git remote add origin "$WIKI_URL"
            git push origin master --force || true
          fi

      - name: 🌈 Generate Wiki (Home as Index + Build History on master)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          META="artifacts/meta/models.json"
          TAG=$(jq -r '.version_tag' $META)
          DATE=$(jq -r '.generated' $META)
          mkdir -p wiki/Builds gh-pages

          # ------------------------------------------------------------
          # 🗂️ 1. Build-specific page
          # ------------------------------------------------------------
          BUILD_FILE="wiki/Builds/${TAG}.md"
          {
            echo "# 📦 AndroidWhispers — Build ${TAG}"
            echo "Generated: ${DATE}"
            echo ""
            echo "## 🧩 Latest APK"
            echo "- [Download app-release.apk](https://github.com/${REPO}/releases/latest/download/app-release.apk)"
            echo ""
            echo "## 🎧 Whisper Models"
            echo "| Model | Size |"
            echo "|:--|--:|"
            jq -r '.models[]? | "| \(.name) | \(.size) |"' $META || echo "| (none) | |"
            echo ""
            echo "## 🕓 Recent Releases"
            JSON=$(curl -s -H "Authorization: token ${GH_TOKEN}" "https://api.github.com/repos/${REPO}/releases?per_page=5" || echo "[]")
            echo "$JSON" | jq -er 'if type=="array" then .[] | "- [" + ((.name // .tag_name // "untagged")) + "](" + (.html_url // "#") + ") (" + ((.published_at // "N/A")[0:10]) + ")" else "- (No releases found)" end' || echo "- (No releases found)"
            echo ""
            echo "<div align='center'><img src='https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${BASE_URL}' width='180'></div>"
          } > "$BUILD_FILE"

          # ------------------------------------------------------------
          # 🧭 2. Generate Wiki Home (Index)
          # ------------------------------------------------------------
          HOME="wiki/Home.md"
          {
            echo "# 🏠 AndroidWhispers Wiki Home"
            echo "Updated: ${DATE}"
            echo ""
            echo "## 🔗 Latest Build"
            echo "- [${TAG}](./Builds/${TAG}.md)"
            echo ""
            echo "## 📜 Build History"
            find wiki/Builds -name '*.md' -type f | sort -r | while read f; do
              name=$(basename "$f" .md)
              echo "- [${name}](./Builds/${name}.md)"
            done
            echo ""
            echo "## 📦 Download Links"
            echo "- [⬇️ Latest APK](https://github.com/${REPO}/releases/latest/download/app-release.apk)"
            echo "- [🌐 GitHub Pages](${BASE_URL})"
            echo ""
            echo "<p align='center'><img src='https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${BASE_URL}' width='180'></p>"
          } > "$HOME"

          # ------------------------------------------------------------
          # 🪄 3. Commit and push (force to master)
          # ------------------------------------------------------------
          cd wiki
          git init
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "🏠 Update Wiki Home (${TAG})"
          git branch -M master
          git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${REPO}.wiki.git"
          echo "🚀 Pushing Wiki updates to master branch..."
          git push origin master --force || (git remote set-url origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${REPO}.wiki.git" && git push origin master --force)

      - name: 💎 Generate GH Pages (Glass UI)
        run: |
          META="artifacts/meta/models.json"
          TAG=$(jq -r '.version_tag' $META)
          DATE=$(jq -r '.generated' $META)
          MODELS=$(jq -r '.models[]? | "<li>\(.name) — \(.size)</li>"' $META)
          mkdir -p gh-pages/models

          # Copy model binaries if present
          cp app/src/main/assets/models/*.bin gh-pages/models/ 2>/dev/null || true

          cat > gh-pages/index.html <<HTML
          <!doctype html><html lang="en"><head>
          <meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/>
          <title>📦 AndroidWhispers Downloads</title>
          <style>
            body{font-family:'Inter',system-ui;background:#0b0f17;color:#eee;margin:0}
            .wrap{max-width:960px;margin:auto;padding:40px}
            .glass{background:rgba(255,255,255,0.08);border-radius:20px;box-shadow:0 8px 40px rgba(0,0,0,0.4);padding:32px;backdrop-filter:blur(14px)}
            a{color:#72d0ff;text-decoration:none}
            footer{margin-top:20px;font-size:.9em;opacity:.7}
          </style></head><body><div class="wrap"><div class="glass">
          <h1>📦 AndroidWhispers</h1>
          <p>Updated: ${DATE}</p>
          <p><a href="https://github.com/${REPO}/releases/latest/download/app-release.apk">⬇️ Download app-release.apk</a></p>
          <p><a href="${WIKI_PAGE_URL}">📘 View Wiki (Build History)</a></p>
          <h3>🎧 Whisper Models</h3>
          <ul>${MODELS}</ul>
          <img src="https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=${BASE_URL}" alt="QR">
          <footer>Generated by AndroidWhispers CI/CD v1.2.2</footer>
          </div></div></body></html>
          HTML

      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: gh-pages
          publish_branch: gh-pages
          force_orphan: true
          commit_message: "🌈 Update GH Pages (AndroidWhispers v1.2.2)"
