# ============================================================
# ? Gradle Wrapper Configuration ? Whispers Project (Final Safe)
# ------------------------------------------------------------
# ? Gradle 8.14.3 (binary distribution)
# ? Compatible with Android Gradle Plugin 8.13.x
# ? Verified URL integrity + longer timeout (for CI)
# ? Stable defaults for reproducible builds
# ============================================================

# Timestamp
#Tue Oct 28 16:28:12 PDT 2025

# ---------------------------
# Distribution (Gradle version)
# ---------------------------
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
# ? Use official Gradle binary distribution (compact version)
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-bin.zip

# ---------------------------
# Network and validation
# ---------------------------
# ? Increase timeout for CI (30 seconds = 30000 ms)
networkTimeout=30000
# ? Ensure distribution URL integrity (Gradle 8+)
validateDistributionUrl=true

# ---------------------------
# Local cache configuration
# ---------------------------
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists

# ============================================================
# ? Debug & Maintenance Tips
# ------------------------------------------------------------
# ? To verify wrapper integrity:
#     ./gradlew wrapper --gradle-version 8.14.3 --distribution-type bin
#
# ? To clean wrapper cache:
#     rm -rf ~/.gradle/wrapper/dists/
#
# ? To view Gradle debug info:
#     ./gradlew tasks --info
# ============================================================
